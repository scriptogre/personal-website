services:
  personal-site:
    build: .
    container_name: personal-site
    restart: unless-stopped
    environment:
      - HOST=0.0.0.0
      - PORT=4321
      - SENDGRID_API_KEY=${SENDGRID_API_KEY}
    expose:
      - "4321"
    networks:
      - proxy

networks:
  proxy:
    external: true
